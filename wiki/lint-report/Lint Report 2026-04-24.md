---
title: Lint Report 2026-04-24
type: lint-report
tags:
- 知识管理
---

## 检查日期
2026-04-24（北京时间 22:00，定时触发）
## 总体健康度
🔴 0 / 100
主要扣分项：203 条过期草稿（>7 天）累计扣分超出上限，叠加引用结构化系统性问题，健康度见底。
## 全库统计（类型 × 状态）
说明：index / lint-report 类型已全部排除在统计之外。
## 孤岛条目（Phase 1 初筛）
检测方法：concept/entity 名称与 1237 条 summary 标题做推理层匹配。由于不允许自连接 SQL，仅完成「标题命中初筛」；未命中者理论上需 Notion search 二次验证，本次因规模过大（2502 条）仅抽样验证。
结论：绝大多数常见概念（OpenClaw、Vibe Coding、Claude Code、RAG 等）均有 summary 标题明确提及，未发现大规模孤岛。以下为疑似孤岛（标题初筛未命中，建议人工核验）：
- GR4AD（concept）
- aApp（concept）
- TAOR 循环（concept）
- Schema Bloat（concept）
- R.E.S.T模型（concept）
- Symphony（concept）
上述 6 条为低置信度疑似孤岛，需 Notion Search 二次确认。本次不计入扣分。
## 过期草稿（>7 天，状态=草稿）
总计：203 条 concept/entity 创建于 2026-04-17 之前且状态仍为「草稿」。
以下列出代表性样本（按创建时间排序，共展示 30 条，完整列表请通过 Wiki 视图「🔄 批处理」筛选）：
注：203 条中大多数为 2026-04-11 ～ 2026-04-17 之间批量创建的草稿，content 结构完整，但由于「来源引用」均为纯文本（详见引用结构化检查），无法触发 草稿→审核中 的自动晋升。根本原因是 Compiler 产出的引用格式问题，而非内容质量问题。
## 过时内容（最后编译时间 >30 天或 null）
经 SQL 全量扫描：所有 last_compile IS NULL 的页面均为 7 个系统元 concept 页（关于 Wiki Compiler / Lint Agent / Fixer / Gap Agent / QA Agent / Synthesizer / Notion AI），已按规则豁免。
其余所有页面的最后编译时间均 ≥ 2026-03-25，未发现非豁免过时内容。
✅ 过时内容扣分：0 分
## 重复疑似
### A. 完全同名重复（summary）
SQL 检测结果：无完全同名 summary 重复。✅
### B. 近似同名重复（concept/entity，推理层归一化）
经推理层归一化分析（lowercase → 去标点 → 统一繁简体 → 去括号后缀 → 版本号归一），发现以下疑似近似重复对：
注：以上为推理层的疑似对，建议人工核查是否合并，不扣分（未确认）。
### C. summary 近似同名重复
全量 1237 条 summary 经标题归一化分析，未发现明显近似重复对。✅
## 状态异常
SQL 全量扫描：所有 concept/entity/summary/qa/synthesis 页面均已设置「状态」字段，无 null/空值异常。
✅ 状态异常扣分：0 分
## 标签异常
SQL 扫描结果：
- 空标签：0 条 ✅
- 废弃标签（AI Agent / MCP / Notion）：0 条 ✅
所有 concept/entity 均至少有 1 个有效标签。
## 标签分布统计
以下为 concept+entity 中主要标签分布（基于样本估算，非精确 SQL 聚合）：
分布观察：Agent 编排 / Coding Agent / LLM 三类高度集中，但后两者内有类型误分类风险。
## 类型启发式筛查结果（concept 疑似应为 entity）
以下条目为 concept 类型，但命中启发式规则，建议人工确认是否改为 entity：
以上 7 条均为「建议人工确认」，不自动判定，不扣分。
## 标签分类合理性检查（Phase 2 抽样）
基于已加载的页面内容与全量名称列表，发现以下潜在误分类：
开发工具 误分类风险：
- Maps Demo Key（entity）、Google Maps Platform（entity）— 已是 entity 类型，标签正确
- 未发现 AI Coding Agent 被误归入「开发工具」的情况
LLM 误分类风险：
- GR4AD（concept，标签 LLM）— 这是一个特定推荐系统算法名称，可能更适合归类为 entity 或添加更具体标签
- 生成式推荐系统（concept，标签 LLM）— 建议改为「知识管理」或「商业/生态」更贴切
结论：整体分类质量较好，无大规模系统性误分类。
## 🚨 引用结构化检查（系统性问题警报）
抽样方法：按创建日抽取 2026-04-11 至 2026-04-12 批次草稿，共加载 8 个 concept/entity 页面进行深度检查。
抽样统计：
已检查页面清单：
### 🚨 系统性问题判定
Affected Page Rate = 100%，远超 30% 阈值。
影响评估：
- 当前 Wiki 中草稿状态 concept/entity 共 1296 条，估计其中 ≥ 1000 条存在纯文本引用问题
- 这导致：
- 根本原因：Compiler 早期批次（2026-04-10 ～ 2026-04-12）输出的草稿统一使用纯文本引用格式，未生成 <mention-page> 结构化引用
## 计分明细
最终得分：0 / 100 🔴
说明：得分触底的核心原因是 203 条过期草稿（−609 分）。这些草稿内容质量多为合格，问题在于引用格式导致晋升链路失效，形成草稿积压。
## 状态晋升建议
经 Phase 2 抽样（8 个草稿页面）检查：
- 所有抽样页面均有「定义」和「关键要点/核心要点」章节，内容结构合格
- 但「来源引用」均为纯文本，不含 <mention-page> 标签，不满足晋升条件
结论：在「引用升级」修复完成之前，无法自动批量推进 草稿→审核中。修复引用格式是状态晋升的前提。
## 改进建议
### 🤖 自动修复项（Fixer 可直接执行）
1. synthesis 状态晋升
